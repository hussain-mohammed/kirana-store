{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash start.sh"
  },
  "environments": {
    "production": {
      "build": {
        "builder": "NIXPACKS",
        "buildCommand": "python -m ensurepip --upgrade && python -m pip install --upgrade pip && python -m pip install -r requirements.txt && python -m pip install psycopg2cffi && chmod +x start.sh"
      },
      "deploy": {
        "startCommand": "bash start.sh"
      }
    }
  }
}
