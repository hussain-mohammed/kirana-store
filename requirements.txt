# All major dependencies now come from Nix packages
# Keep only dependencies not available in nixpkgs
python-multipart==0.0.6
mangum==0.17.0
# All other packages (fastapi, psycopg2, sqlalchemy, etc.) come from nixpkgs
