[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "python312Packages.psycopg2"]
depsPath = "requirements.txt"

[phases.install]
cmds = []

[phases.build]
cmds = ["chmod +x start.sh"]

[start]
cmd = "bash start.sh"
